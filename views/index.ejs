<!DOCTYPE html>
<html>
  <%- include("partials/head") %>
  <body class="h-screen flex items-center justify-center bg-gradient-to-br from-sky-400 to-teal-500">
    <div class="text-center">
      <h1 class="text-3xl font-bold text-white mb-6 drop-shadow-lg">Get Weather Results!</h1>
      <form action="/forecast" method="get" 
            class="flex bg-white/80 backdrop-blur-md rounded-xl overflow-hidden shadow-lg w-96 mx-auto">
        <input type="text" name="q" placeholder="Enter city..." 
               class="flex-grow px-4 py-3 text-gray-700 focus:outline-none" required>
        <button type="submit" 
                class="bg-teal-600 text-white px-6 py-3 hover:bg-teal-700 font-semibold">
          Search
        </button>
      </form>
    </div>
  </body>
</html>
